{
  "name": "cricket-chatgpt-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "npm run build && npm start",
    "ui:build": "cd ui && npm run build",
    "dev:all": "npm run ui:build && npm run dev"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "clean": "^4.0.2",
    "cors": "^2.8.5",
    "express": "^4.19.0",
    "vite": "^7.2.7",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  }
}
