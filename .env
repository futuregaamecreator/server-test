# Leave BASE_URL unset for local dev — the server auto-launches a Cloudflare
# quick tunnel on startup and fills this in from the URL it's assigned.
# Set it explicitly only when deploying behind a fixed/real domain.
# BASE_URL=
PORT=8000